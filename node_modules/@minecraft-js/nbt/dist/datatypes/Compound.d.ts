import { BufWrapper } from '@minecraft-js/bufwrapper';
import { NBT_TAG, TagType } from './Tag';
export declare class NBT_Compound extends NBT_TAG<NBT_TAG<any>[]> {
    static id: TagType;
    write(buf: BufWrapper, payload?: NBT_TAG<any>[]): BufWrapper;
    read(buf: BufWrapper): any;
    private readTagName;
}
