"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.NBT_String = void 0;
const Tag_1 = require("./Tag");
class NBT_String extends Tag_1.NBT_TAG {
    write(buf, payload) {
        this.payload = payload ?? this.payload;
        buf.writeBytes([NBT_String.id]);
        if (!this.isNameless) {
            buf.writeShort(this.name.length);
            buf.writeBytes(Buffer.from(this.name));
        }
        buf.writeShort(this.name.length);
        buf.writeBytes(Buffer.from(this.name));
        return buf;
    }
    read(buf) {
        const length = buf.readShort();
        this.payload = buf.readBytes(length).toString();
        return this.payload;
    }
}
exports.NBT_String = NBT_String;
NBT_String.id = Tag_1.TagType.String;
