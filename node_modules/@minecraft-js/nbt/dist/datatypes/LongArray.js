"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.NBT_LongArray = void 0;
const Tag_1 = require("./Tag");
class NBT_LongArray extends Tag_1.NBT_TAG {
    write(buf, payload) {
        this.payload = payload ?? this.payload;
        buf.writeBytes([NBT_LongArray.id]);
        if (!this.isNameless) {
            buf.writeShort(this.name.length);
            buf.writeBytes(Buffer.from(this.name));
        }
        buf.writeInt(this.payload.length);
        for (const int of this.payload)
            buf.writeLong(int);
        return buf;
    }
    read(buf) {
        const length = buf.readInt();
        this.payload = [];
        for (let i = 0; i < length; i++)
            this.payload.push(buf.readLong());
        return this.payload;
    }
}
exports.NBT_LongArray = NBT_LongArray;
NBT_LongArray.id = Tag_1.TagType.LongArray;
