import { BufWrapper } from '@minecraft-js/bufwrapper';
import { NBT_TAG, TagType } from './Tag';
export declare class NBT_Short extends NBT_TAG<number> {
    static id: TagType;
    write(buf: BufWrapper, payload?: number): BufWrapper;
    read(buf: BufWrapper): number;
}
