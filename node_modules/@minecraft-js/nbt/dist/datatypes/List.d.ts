import { BufWrapper } from '@minecraft-js/bufwrapper';
import { NBT_TAG, TagType } from './Tag';
export declare class NBT_List extends NBT_TAG<NBT_TAG<unknown>[]> {
    static id: TagType;
    write(buf: BufWrapper, payload?: NBT_TAG<unknown>[]): BufWrapper;
    read(buf: BufWrapper): NBT_TAG<unknown>[];
}
